import { UniqueLocationPipe } from './unique-location.pipe';

describe('UniqueLocationPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueLocationPipe();
    expect(pipe).toBeTruthy();
  });
});
